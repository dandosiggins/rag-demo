[phases.setup]
nixPkgs = ["nodejs_22", "pnpm-9_x", "openssl"]

[phases.install]
cmds = ["pnpm install --no-frozen-lockfile"]

[phases.build]
cmds = ["PORT=3000 BASE_PATH=/ pnpm --filter @workspace/rag-demo build && pnpm --filter @workspace/api-server build"]